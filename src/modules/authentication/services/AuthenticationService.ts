class AuthenticationServices {
  async execute(email: string, password: string): Promise<void> {
    const authenticated: [] = [];

    const teste = Object.assign({
      email,
      name,
    });

    // this.authenticated.push(teste);
  }
}

export { AuthenticationServices };
